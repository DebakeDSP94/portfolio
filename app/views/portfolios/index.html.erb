<div class="album text-muted">
  <div class="container">
      <%= source_helper("alert alert-warning") %>
    <div class="row sortable" data-controller="draggable" data-draggable-resource-value="portfolio_item" data-drag-url-value="" data-draggable-attribute-value="position" data-draggable-target="list">
      <%= render partial: 'portfolio_item', collection: @portfolio_items.order(position: :asc) %>
    </div>
  </div>
</div>