<div class="album text-muted">
  <div class="container">
    <div class="row sortable" data-controller="drag" data-drag-target="list">
      <%= render partial: 'portfolio_item', collection: @portfolio_items.order(position: :asc) %>
    </div>
  </div>
</div>