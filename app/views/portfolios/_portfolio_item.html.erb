<div data-id="<%= portfolio_item.id %>"  class="card portfolio-card col-md-4">
  <p class="card-text">
    <span>
      <%= link_to (image_tag (portfolio_item.main_image_url(:thumb).to_s), width: '100%'), portfolio_show_path(portfolio_item) %>
    </span>
      <%= portfolio_item.subtitle %>
  </p>
</div>