  <h3>Comments</h3>

<% commentable.comments.each do |comment| %>
  <div class="card comment-card">
    <div class="card-body bg-light comment">
      <%= comment.body %>
    </div>
  </div> 
<% end %>
